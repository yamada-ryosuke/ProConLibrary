#include <bits/stdc++.h>

//////////////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////// ここからコピペ ////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////////////////////////////////

////////////////////////////
// グラフの辺のテンプレート //
////////////////////////////

// 使うライブラリごとに中身を書き換える
// 複数ライブラリを使うときは統一初期化記法の部分でバグる場合があるので、その場合ライブラリの方も書き換える必要があることに注意
// TODO: 統一初期化記法に頼らず関数とメソッドチェーンで初期化できるようにする
// お気持ちポリシー
struct Edge {
	int to;
	int64_t dist{1};

	int64_t cost;
	int64_t capacity;
	int rev_i;
};
using EdgeVec = std::vector<Edge>;
using EdgeLists = std::vector<EdgeVec>;